<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Formula_gases.burgerMenu">
    <MasterDetailPage.Master>
        <ContentPage
            Title="Calculadora gases"
            Icon="">
            <StackLayout Orientation="Vertical">
                <ListView x:Name="listMenu" SeparatorColor="Gray">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextCell Text="{Binding menuTitle}" Detail="{Binding menuDetail}" TextColor="#0F7173"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ContentPage>
    </MasterDetailPage.Master>
</MasterDetailPage>